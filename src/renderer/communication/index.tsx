import React from 'react';

/**
 * Simple component, which main job is to transfer messages from other components to backend
 */
const Communicator: React.FC = () => {
  return <></>;
};

export default Communicator;
