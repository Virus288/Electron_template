import React from 'react';

const Home: React.FC = () => {
  return (
    <div>
      <button type="button" className="mainButton">
        Button
      </button>
    </div>
  );
};

export default Home;
